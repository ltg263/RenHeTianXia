<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jxxx.rhtx">

    <application
        android:name=".app.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RenHeTianXia">
        <activity android:name=".view.activity.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.LoginActivity" />
        <activity android:name=".view.activity.LoginWjmmActivity" />
        <activity android:name=".view.activity.SetUserXbActivity" />
        <activity android:name=".view.activity.SetUserSgActivity" />
        <activity android:name=".view.activity.MineSetActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>